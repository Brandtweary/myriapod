{
  "name": "cymbiont-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Metering inference proxy for cymbiont-web: holds the owner OpenRouter key, meters per-principal usage, gates free credits. Self-contained Bun service — deploy by copying proxy/ and running `bun install && bun start`.",
  "scripts": {
    "start": "bun run server.ts",
    "mint-code": "bun run mint-code.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@cap.js/server": "^2.0.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.7.3"
  }
}
